# Cadence 1.7 site acceptance

## Current displays and copy, September 20

Every app view on the page now comes from the shared 1.7 frontend: desktop and phone Write, Sync, the correction controls, Rhyme Lab and Share Studio. The captures use disposable local profiles. Sync shows a labeled sample connection with external requests blocked; it is not evidence of a provider test. No private writing or account details appear in the assets.

The four-line owner verse, punctuation, 19 marks, four families and 76% word coverage are unchanged. Lab keeps the `crazy broke` query, actual result order and the `maybe` / `both` combination. Share Studio uses the separately approved eight-word phrase, with the engine identifying `Write`, `recite` and `revise` and the export palette keeping them violet. Every feature capture opens at full size.

The surrounding copy and 1.7 release notes now follow the owner's About voice. Storage guidance still distinguishes the local SQLite database, readable Markdown exports and device-local histories. It still requires updating connected devices before using 1.7 sync and installing Android updates over the existing app.

Six content tests and browser checks at 360, 390, 820 and 1440 pixels pass, including keyboard/touch targets, no-JavaScript use, resource loading and reduced motion. Browser captures now explicitly decode lazy images before taking full-page screenshots. The seven new images, including the 1200 by 630 social preview, were visually inspected. App release-copy tests and TypeScript pass. The in-app changelog is a source update for the next binary; the released installers are unchanged.

## Original publication

The owner authorized publication and the matching site deployment on September 20, 2026. The public 1.7 release was published at 04:16:37 UTC. All 19 files passed anonymous download and SHA-256 verification before the site merge; signed binaries match the accepted staged build. The live updater endpoint and all nine platform entries pass, and prior releases remain available.

The branch points Windows, Android, other desktop packages and checksums to v1.7.0. Setup guidance describes the active local database, readable Markdown/Git folder, complete backups and the current Settings > Sync location. The release label and social image are updated. Owner-authored lyrics, punctuation, line breaks and all 19 engine-derived marks are unchanged.

Six content tests pass. Browser acceptance passes at 360, 390, 820 and 1440 pixels, including navigation, keyboard use, touch targets, resources, JavaScript-disabled behavior and the existing reduced-motion/animation checks. The regenerated 1200 by 630 social image was visually checked.

Platform links match the verified public asset names. Promotion generated the checksum index and rewrote updater URLs without rebuilding binaries. After deployment, check the live HTML, styles, scripts, media and signed-out download links. The application release record owns source identity, installed candidate evidence, deployment results and limitations.
